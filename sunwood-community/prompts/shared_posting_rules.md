- 投稿アカウントは `@Onizuka_Renji` を使ってください。
- 投稿の実行は `skills/sunwood-community/scripts/post_thread.py` を使ってください。
- 本体投稿、画像添付、複数リプライは `payload JSON` にまとめて、`post_thread.py --payload-file ...` を1回だけ実行してください。
- 本体投稿やリプライを個別コマンドや手作業でばらばらに投稿しないでください。
- 本文では `$ONIAGI` を使ってください。
- 先に解説ポストを投稿してください。
- 本文には URL を入れないでください。
- 本文やリプライの文字列に、`\n`、`\r`、`\t` のような生のエスケープ列をそのまま含めないでください。
- 参照URLは、必ずその自分の投稿へのリプライに貼ってください。
- 複数の参照URLを同じリプライにまとめないでください。用途ごとにリプライを分けてください。
- 複数のリプライが必要な場合は、`post_thread.py` の `replies` 配列に分けて入れてください。必要なら `reply_to` で `main` または先行リプライを指定してください。
- リプライは URL だけ貼らず、何のリンクなのか、なぜ読む価値があるのかを短く補足してください。
- 参照URLのリプライには、その論点や読む目的を1から2文で添えてください。
- 投稿には、ざっくり内容が伝わる図解画像を必ず添付してください。
- 本体投稿に画像が付いていない状態はエラーです。画像が用意できない場合は投稿しないでください。
- 図解画像は `memory/docs/public/Onizuka_2k_delpmaspu.png` を参照画像として使い、`nano-banana-2 edit` 系の手順で作成してください。
- 図解画像は文字を詰め込みすぎず、3から5個の要点をアイコン、矢印、短いラベルで見せるラフな explainer にしてください。
